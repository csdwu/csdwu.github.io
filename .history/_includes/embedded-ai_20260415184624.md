{% assign page_data = site.data.embedded_ai %}
{% assign papers_data = site.data.embedded_ai_papers %}

## {{ page_data.title }}

{% for paragraph in page_data.intro.paragraphs %}
{{ paragraph }}

{% endfor %}

## Useful Links

<ul>
  {% for link in page_data.useful_links %}
    <li>
      <a href="{{ link.url }}" target="_blank" rel="noopener">{{ link.title }}</a>
      {% if link.description %} — {{ link.description }}{% endif %}
    </li>
  {% endfor %}
</ul>

## Last Updated

{% if papers_data and papers_data.last_updated %}
  {{ papers_data.last_updated | date: "%B %-d, %Y" }}
{% else %}
  {{ page_data.last_updated | date: "%B %-d, %Y" }}
{% endif %}

## Papers

{% if papers_data and papers_data.paper_sources and papers_data.paper_sources.size > 0 %}
  {% for source in papers_data.paper_sources %}
### {{ source.title }}

    {% for category in source.categories %}
#### {{ category.name }}

      {% if category.papers and category.papers.size > 0 %}
<ul>
        {% for paper in category.papers %}
          {% assign paper_link = paper.pdf_url | default: paper.venue_url %}
  <li>
    <a href="{{ paper_link }}" target="_blank" rel="noopener">{{ paper.title }}</a>
    {% if paper.venue %} — <em>{{ paper.venue }}</em>{% endif %}
    {% if paper.month and paper.month != "" %}
      ({{ paper.month }} {{ paper.year }})
    {% elsif paper.year %}
      ({{ paper.year }})
    {% endif %}
  </li>
        {% endfor %}
</ul>
      {% else %}
No papers have been added to this category yet.
      {% endif %}

    {% endfor %}
  {% endfor %}
{% else %}
No paper data is available yet.
{% endif %}